import { createContext } from "react";

export const Context = createContext(); 

// export const EmptyContext = createContext("No data is passing here") //no data passing here but we write here to display if no pro